# 目标
git的核心概念
分支与合并
日常工作流与冲突解决

## D31
### git存的是什么？
git 存的是快照（snapshot）。 每次 commit 记录的是"整个项目在这一刻长什么样"的完整目录树。在 git log -p、git diff 里看到的差异，是 git 现场算出来的，不是它存储的形式。
git的存储是，内容寻址，文件内容选一个SHA -1，用这个hash当做文件名存起来，这就让存储天然具备了去重的优点，内容不变，hash不变，也就是同一个对象，不用再存。这样就导致git中的存储并不大。
git 是个内容寻址的键值数据库，键是内容的 hash，值是内容本身。上层那些 add/commit/branch 都是在这个数据库上糊的壳。

.git/objects 里只住四种东西
|对象	|存什么	|关键点|
|----|-----|-----|
|blob	|一个文件的内容	|只有内容。不含文件名、不含路径、不含权限|
|tree	|一个目录	|一张表：权限 类型 hash 文件名，指向 blob 或子 tree|
|commit	|一个 tree 的 hash + 父 commit 的 hash + 作者 + 提交者 + 说明	|快照的入口|
|tag（附注）	|指向某对象的带签名/说明的标签|	平时用得少|

有一个很反直觉的点，文件名不存在blob中，而是在tree里
如果把 D6.md 改名成 week6.md、内容一个字没动——blob 的 hash 不变（内容没变），变的只是 tree 里那一行的文件名。git 不记录"重命名"这个动作

再看层级关系，一个commit是这样的一颗树的根
```
commit  ──► tree (仓库根目录)
              ├── blob    roadmap.md
              ├── blob    D6.md
              └── tree    .vscode/     ──► blob  settings.json
```

hash从哪来？
git 算 hash 不是直接对文件内容算，而是先拼一个头：
>"<类型> <字节数>\0<内容>"   →  SHA-1  →  40 位十六进制

比如一个内容是 hello 的文件，实际被 SHA-1 的是 blob 5\0hello。
commit 对象也一样，它被 hash 的"内容"长这样:
tree 8a3c9f...
parent 198cb37...
author  dadadasg <mail> 1755610205 +0800
committer dadadasg <mail> 1755610205 +0800
所以可以推断出三个结论，
1、commit 的时间戳是被 hash 的内容的一部分 → 改时间 = 改内容 = 必然换一个新 hash。改时间这件事，本质上不是"修改"，是"另造一个 commit 然后丢掉旧的"。
2、commit 里存着 parent 的 hash → 你改了某个 commit，它的 hash 变了，它儿子里记的 parent 就对不上了，儿子必须重造 → 孙子也得重造……从被改的那个点往后，整条链全部换 hash。这就是"改历史"代价的来源，也是为什么改完必须 push -f。
3、有两个时间字段：author（作者时间）和 committer（提交者时间）。它们是两个独立的字段，可以不一样。
### 工作区 / 暂存区 / 仓库

|区|	物理上是什么|
|--|----|
|工作区	|目录里那些普通文件，跟 git 没关系，就是文件|
|暂存区	|.git/index，一个二进制文件。它是一张表：路径 → blob hash + 元数据。不是文件夹|
|仓库	|.git/objects 里的对象库|

所以git add x.md实际是把x.md的内容立即写成一个blob存进.git/objects，然后在.git.index表上登记x.md->这个hash，也就是说在add这个部分，内容已经存到对象库中了，这也解释了为什么文件损坏了还能通过git checkout -- x.md找回来
git commit是把.git.index这个表冻结成tree对象 → 造一个 commit 对象指向这棵树、指向当前的父 commit → 把当前分支的指针挪到新 commit。
暂存区不是"待提交的文件副本"，是一张登记表。

### 分支和HEAD真身
分支 = 一个文件，里面就一行 41 字节（40 位 hash + 一个换行）。位置：.git/refs/heads/main。
HEAD = .git/HEAD，内容通常是 ref: refs/heads/main——它是指针的指针，记录"你现在站在哪个分支上"。

建分支 = 写一个 41 字节的新文件。
切分支 = 改 .git/HEAD 那一行 + 把工作区文件换成对应快照。
"detached HEAD"（游离头指针）也不神秘：.git/HEAD 里直接写了一个 40 位 hash，而不是 ref: refs/heads/xxx——你站在一个 commit 上，没站在任何分支上。

## D32
在commit的时候，git做了三步：
1、造出新 commit 对象，它的 parent 写成"当前 HEAD 指向的那个 commit"
2、把当前分支文件里那 40 位 hash 改写成新 commit 的 hash
3、HEAD 不用动（它指的是分支名，不是 commit）
以自己仓库举例，我昨天上传了一个新的提交
hash值是8d312d3...

硬盘上的两个文件是这样的
.git/HEAD
ref: refs/heads/main
.git/refs/heads/main
8d312d3...

当我添加了D7.md到暂存区之后
D7.md的内容被算成了SHA-1，写到.git/objects中成为一个blob
.git/index这个表中多出一行：D7.md指向那个blob的hash

然后我再提交
先把index表冻成tree，index表中有12行，一共12个提交，假设这个tree的hash是t111...
其它11个blob的hash和上一个commit是一样的，内容没变，只有tree是新建的
然后造commit对象，git要给新的commit找一个父节点，
先去.git/HEAD中找，发现它指向ref: refs/heads/main，
再去.git/refs/heads/main，发现它指向8d312d3...，这个就是parent
所以这个commit的内容就是
tree t111...
parent 8d312d3...
author ...
committer ...

上传第七天的笔记

一整个内容算SHA-1，得到一个新的hash，假设是c111...

然后挪指针，git把.git/refs/heads/main中的内容改成新的hash，c111...
这样就commit成功了

### 合并
假设你在 main 上，要 git merge feature。git 先做一件事：找 merge base（最近共同祖先）。然后分三种情况：

情况一：已经合过了 —— feature 就是 main 的祖先，什么都不用干，Already up to date。

情况二：fast-forward（快进） —— main 就是 merge base，也就是说 main 一步没动，feature 单方面往前跑了：
```
        main
         ↓
A ── B ── C ── D ── E
                    ↑
                 feature
```
这时候合并根本不需要"合"，git 只要把 main 这个文件里的 hash 改成 E。没有新 commit 产生，没有算法参与，纯粹挪指针。这就是"快进"这个词的来历——像磁带快进，没有分叉可言。

--no-ff 就是强行不许它偷懒，硬造一个合并 commit，好让历史上看得出"这里曾经有个分支"。
情况三：真合并（three-way merge） —— 两边各自都有了新 commit：
```
              D ── E   ← main
             /
A ── B ── C
             \
              F ── G   ← feature
```
merge base 是 C。git 要造一个有两个 parent 的 commit：
```
              D ── E ─────── M   ← main
             /              /
A ── B ── C                /
             \            /
              F ──────── G   ← feature
```
M 的对象内容里会有两行 parent（E 和 G）。这是 commit 对象唯一允许"多个父亲"的场合，也是 git log --graph 里那些分叉线的物理来源。
三方指的是:base(C)、ours(E)、theirs(G),git对每个文件的每一块(hunk)做判断:

|base	|ours	|theirs	|结果|
|-------|-------|-------|---|
|有	|没改|	改了|	要 theirs 的|
|有	|改了|	没改|	要 ours 的|
|有	|改了|	改成一样的|	要那个（不算冲突）|
|有	|改了|	改成不一样的|	冲突|

有 base 才能判断谁"改了"。两个版本摆在面前是分不出谁动过的，必须有个共同祖先当参照物。
粒度是 hunk，不是文件。你和同事都改了 readme.md，但你改开头他改结尾 → 两块互不相干，各自套用规则，不冲突，git 自动合。
当冲突产生时，暂存区的表中，这个路径存了三条记录(stage 1/2/3=base/ours/theirs)，
当手动改完冲突文件后敲git add，三条记录坍缩成一条，冲突状态解除

rebase
rebase：它不是"移动"，是"重放"
git rebase main在feature上的实际操作：
1、找到 merge base，算出你独有的那几个 commit（F、G）
2、把每个 commit 算成一个 diff（补丁）
3、HEAD 挪到 main 的最新 commit（E）
4、把那些补丁一个一个重新打上去，每打一个就造一个全新的 commit

```
              D ── E              ← main
             /       \
A ── B ── C           F' ── G'    ← feature（F'、G' 是全新对象）
             \
              F ── G              ← 旧的，没人引用了，等 GC
```
F' 和 F 是两个不同的对象：内容可能一样，但 parent 变了 → hash 必然不同（D31 的结论）。旧的 F、G 还躺在对象库里，但没有任何 ref 指向它们，只有 reflog 记着它们的地址。
rebase 会把 committer date 刷成"现在"，但保留原来的 author date。
rebase 冲突可能要解好几次——因为它是一个 commit 一个 commit 重放的，每一步都可能撞车。而 merge 只解一次
通常使用merge，rebase是给别人展示用的

## D33
### 远程仓库：origin/main
远程仓库，GitHub上的linux_study的仓库，本质上就是另一个git仓库，不同的是它是bare仓库，没有工作区
origin是一个昵称，存在.git/config中，只是一串URL缩写，可以叫他github、abc。

origin/main不在服务器上，它是本地的一个文件
>.git/refs/remotes/origin/main      ← 又是一行 40 位 hash

它的意思是，上一次和服务器联系时，服务器的main停在这儿
所以本地实际上有两个东西，
main放在.git/refs/heads/main中，是我自己的书签
origin/main放在.git/refs/remotes/origin/main中，是我对服务器的记忆
而真正的远程main在服务器的硬盘上
所以git status的状态是git离线算出来的，它只需要对比.git/refs/heads/main和.git/refs/remotes/origin/main看看有没有发生改变即可，也就是说它是对真实的服务器一无所知的

fetch和pull
```
git fetch  =  下载缺的对象  +  刷新 origin/main 这份记忆
              ↑ 完全不碰 main，不碰工作区，不碰 index —— 绝对安全

git pull   =  git fetch  +  git merge origin/main
              ↑ 安全的第一步            ↑ 会动东西的第二步
```

push的本质是请求远程做一次fast-forward
git push 干两件事：

1、把本地有、远程没有的对象传上去
2、请求服务器把它的 refs/heads/main 改写成新 hash
但是服务器做快进操作有一条规矩
只有当要推的commit，是远程 main 当前 commit 的后代（即这次改写是 fast-forward），才允许。
假设远程是这样的，有人推了x上去
```
A ── B ── X          ← 远程 main
      \
       C ── D        ← 你本地 main
```
直接push的话，会把远程的main指向D，X就没有任何指针指向它了，别人的工作就消失了
正确的做法是，先用git pull把X拿下来合并，让D变成X的后代，这时候再push

强推
有一种情况必然是 non-fast-forward，而且理直气壮：

已经改了 push 过的历史——比如 rebase 了，或者改了 commit 的时间。

改时间 = 换 hash = 从那个点往后整条链全是新对象。这时候新链和远程的旧链天生对不上，只能强推。

两个强推命令，差别是天壤之别：


git push --force              # 我不管，照我说的改
git push --force-with-lease   # 只有当远程 main 还停在我记忆里那个位置时，才照办
--force-with-lease 的原理是把本地的 origin/main 一起发过去，服务器先核对"我现在的 main 是不是等于记忆里的那个"。

相等 → 说明你 fetch 之后没人动过 → 放行
不等 → 说明有人在不知道的时候推了东西 → 拒绝，保住那个人的工作
改历史时永远用 --force-with-lease，别用 --force。

git push origin main 的完整形态

它是个缩写，全写是：

```
git push origin refs/heads/main:refs/heads/main
#                ↑ 本地的源        ↑ 远程的目标
```
冒号左边右边这个结构叫 refspec。知道它以后，几个"魔法"就不神秘了：

```
git push origin :feature          # 左边是空 → 把"空"推给远程的 feature → 删除远程分支
git push origin HEAD:main         # 把当前位置推到远程 main，不管本地分支叫啥
git push origin dev:main          # 本地 dev 推成远程的 main
```
而 git push -u origin main 里的 -u，是往 .git/config 写一行"main 的上游是 origin/main"。有了它，以后裸敲 git push / git pull git 才知道该找谁。
