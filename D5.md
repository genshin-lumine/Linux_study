# 目标

- **D21** 环境变量与 PATH
- **D22** .bashrc、alias、历史命令技巧
- **D23** 变量与引号（单引号 vs 双引号）
- **D24** 条件与循环入门
- **D25** 复盘日：写第一个小脚本

## D21
### 环境变量
登录后的 shell 是一个进程，而每个进程出生时都随身带着一组名字=值的配置包，这就是环境变量。
先看环境变量里都有些什么
```
env | head -20

SHELL=/bin/bash
PWD=/home/dev
LOGNAME=dev
XDG_SESSION_TYPE=tty
HOME=/home/dev
LANG=en_US.UTF-8
LS_COLORS=rs=0:di=01;34:ln=01;36:mh=00:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:mi=00:su=37;41:sg=30;43:ca=00:tw=30;42:ow=34;42:st=37;44:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arc=01;31:*.arj=01;31:*.taz=01;31:*.lha=01;31:*.lz4=01;31:*.lzh=01;31:*.lzma=01;31:*.tlz=01;31:*.txz=01;31:*.tzo=01;31:*.t7z=01;31:*.zip=01;31:*.z=01;31:*.dz=01;31:*.gz=01;31:*.lrz=01;31:*.lz=01;31:*.lzo=01;31:*.xz=01;31:*.zst=01;31:*.tzst=01;31:*.bz2=01;31:*.bz=01;31:*.tbz=01;31:*.tbz2=01;31:*.tz=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.war=01;31:*.ear=01;31:*.sar=01;31:*.rar=01;31:*.alz=01;31:*.ace=01;31:*.zoo=01;31:*.cpio=01;31:*.7z=01;31:*.rz=01;31:*.cab=01;31:*.wim=01;31:*.swm=01;31:*.dwm=01;31:*.esd=01;31:*.avif=01;35:*.jpg=01;35:*.jpeg=01;35:*.mjpg=01;35:*.mjpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.svg=01;35:*.svgz=01;35:*.mng=01;35:*.pcx=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.m2v=01;35:*.mkv=01;35:*.webm=01;35:*.webp=01;35:*.ogm=01;35:*.mp4=01;35:*.m4v=01;35:*.mp4v=01;35:*.vob=01;35:*.qt=01;35:*.nuv=01;35:*.wmv=01;35:*.asf=01;35:*.rm=01;35:*.rmvb=01;35:*.flc=01;35:*.avi=01;35:*.fli=01;35:*.flv=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.yuv=01;35:*.cgm=01;35:*.emf=01;35:*.ogv=01;35:*.ogx=01;35:*.aac=00;36:*.au=00;36:*.flac=00;36:*.m4a=00;36:*.mid=00;36:*.midi=00;36:*.mka=00;36:*.mp3=00;36:*.mpc=00;36:*.ogg=00;36:*.ra=00;36:*.wav=00;36:*.oga=00;36:*.opus=00;36:*.spx=00;36:*.xspf=00;36:*~=00;90:*#=00;90:*.bak=00;90:*.crdownload=00;90:*.dpkg-dist=00;90:*.dpkg-new=00;90:*.dpkg-old=00;90:*.dpkg-tmp=00;90:*.old=00;90:*.orig=00;90:*.part=00;90:*.rej=00;90:*.rpmnew=00;90:*.rpmorig=00;90:*.rpmsave=00;90:*.swp=00;90:*.tmp=00;90:*.ucf-dist=00;90:*.ucf-new=00;90:*.ucf-old=00;90:
SSH_CONNECTION=114.229.253.182 7561 172.30.4.82 22
LESSCLOSE=/usr/bin/lesspipe %s %s
XDG_SESSION_CLASS=user
TERM=xterm-256color
LESSOPEN=| /usr/bin/lesspipe %s
USER=dev
SHLVL=1
XDG_SESSION_ID=858
XDG_RUNTIME_DIR=/run/user/1000
SSH_CLIENT=114.229.253.182 7561 22
XDG_DATA_DIRS=/usr/local/share:/usr/share:/var/lib/snapd/desktop
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus

# 查看单个的环境变量
printenv HOME
/home/dev

echo $HOME $USER $SHELL
/home/dev dev /bin/bash
```
printenv HOME 是只查 HOME 这一个变量的值；echo $HOME 里的 $ 是取值符——意思是"把 HOME 这个变量的值拿出来"，就是一个变量和普通文本的分界线
观察上述输出，不难发现，home是家目录，user是当前登录身份，shell是正在使用的shell的绝对路径

### path
思考一个问题，ls、vim、sudo这些命令在敲的时候没有指定目录，shell是怎么找到的呢？
答案是用path，下面看一下path
```
echo $PATH
/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin
```

观察输出，PATH的值是用冒号分隔的目录列表，可以用echo $PATH | tr ':' '\n' 把它竖起来看
```
which ls vim

/usr/bin/ls
/usr/bin/vim
```
观察输出，发现ls、vim都在PATH值中
当输入ls命令回车后，shell拿着ls名字，去PATH值中的目录从左到右找，在/usr/bin中找到了可执行文件ls，然后运行它，如果没找到，就会输出command not found
所以PATH是shell的寻路表
有一个推论，PATH的顺序就是命令的优先级，如果有同名的命令，那么排在前面的可以得到执行
为什么which cd输出为空？看一下cd和ls 的区别，用type命令，可以看到更全面的信息
```
type cd
cd is a shell builtin       # 表明cd是一个内建命令
type ls
ls is aliased to `ls --color=auto'
```
所以cd没有自己的文件，它就存在/bin/bash程序里
回忆一下shell是怎么运行外部命令的，首先需要一个子进程来运行，如果cd是外部命令的话，那么输入cd命令后，shell分配一个子进程运行cd，然后这个子进程修改了工作目录，然后完成任务退出，目录改动一起消失了，但是shell目录不动。
子进程改不了父进程的状态，所以cd必须内建
同理还有pwd、history、echo

### 变量怎么传给子进程

先猜输出，再运行
```
NAME=hello  # 没输出
echo $NAME  # 输出hello
hello
```
第一步是在shell里定义了一个NAME变量，echo是输出NAME的值

```
bash -c 'echo 子进程看到的NAME是：[$NAME]'
子进程看到的NAME是：[]
```

bash -c：它开启一个全新的 bash 子进程，执行单引号里的命令后立即退出，方括号里是空的，因为是全新的子进程，所以看不到 NAME。

```
export NAME
bash -c 'echo 子进程看到的NAME是：[$NAME]'
子进程看到的NAME是：[hello]

env | grep NAME     # 查看环境变量
NAME=hello
LOGNAME=dev
```

顺带一提，输出里混进一个 LOGNAME：grep 匹配的是子串，LOGNAME 里也含 NAME——它本来就记录着登录名。

分析一下原因，第一次NAME变量定义的是shell，只在shell里有效，export NAME是允许遗传给子进程，也就是在shell每次 fork子进程后，会把自己的环境变量复制一份，所以环境变量=被export过的变量
注意细节：
1、export NAME=hello 可以一步到位。
2、等号两边不能有空格——NAME = hello 会报错。
重点：**子进程的一切都影响不了父进程**

因为NAME变量只在之前的shell中定义的，所以在新的shell中是没有办法继承过来的，如果想让NAME的变量在每个新会话都自动存在的话，需要把export NAME=hello写进~/.bashrc文件中。
搞一个自己的命令目录，验证PATH机制
```
mkdir ~/bin
cp /bin/ls ~/bin/myls

myls
Command 'myls' not found
```
~/bin 是 Unix 的老传统——用户自己的命令目录。这里没有写脚本，只是把现成的 ls 复制一份、改个名字 myls。
运行myls，发现是command not found，表明PATH的目录中没有这个命令，改用~/bin/myls试试
出现bin  quiz.txt  replace.txt  vim-week-notes.md  words.txt，与ls结果一致

把myls接入PATH
```
PATH=$PATH:$HOME/bin    # 设置路径
myls
bin  quiz.txt  replace.txt  vim-week-notes.md  words.txt
which myls
/home/dev/bin/myls
```
右边用到了 $PATH——变量可以在赋值语句的右侧使用（先取值、拼上新目录、再整体存回去），这就是"追加"，写成 PATH=$HOME/bin，就是把 PATH 整个覆盖成只剩一个目录——那之后连 ls、sudo 都找不到，因为 /usr/bin 从寻路表上抹掉了。
但是这个更改只是临时的，修改只发生在当前进程的内存里，子进程会继承它；但新会话从登录进程继承的是原版——内存一关就清零，磁盘上什么都没变。


## D22
### .bashrc
先看一下在登录的时候发生了什么
先看看家目录中的隐藏文件
```
ls -a ~
.   .bash_history  .bashrc  .cache    quiz.txt     .ssh                       .viminfo  vim-week-notes.md
..  .bash_logout   bin      .profile  replace.txt  .sudo_as_admin_successful  .vimrc    words.txt
```
既然环境变量是登录时被塞进shell的，那么是谁塞的？
.bashrc         # 交互式 shell 的个人配置，当天的主角
.profile        # 登录总帐
.bash_history   # 历史命令的存档
.bash_logout    # 退出时记录

```
cat ~/.profile  # 看一下内容是什么

# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi
```
观察输出，发现有一行注释是if running bash....，意思是如果执行的是bash，就把.bashrc执行一遍
所以 Ubuntu 上的调用链是：**SSH 登录 → 读 .profile → 由 .profile 调用 .bashrc**
然后把PATH写进.bashrc
```
vim ~/.bashrc

# 在最后一行写下,保存退出
export PATH=$PATH:$HOME/bin

source ~/.bashrc    # 重新加载一下配置
myls
bin  quiz.txt  replace.txt  vim-week-notes.md  words.txt
```
.bashrc 只在 shell 出生那一刻被读一次，改完文件它并不会自动重读。source ~/.bashrc 的意思就是"立刻把这个文件执行一遍"。
写入.bashrc的export不能省
source 和 . 都是 bash 的内建命令，功能一样，但 source 是 bash 的方言，不是所有 shell 都有；. 是 POSIX 的通用写法，所以在 .profile 里用更通用的 . 而不是 source

### alias
看一下这个命令alias
```
alias
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'
alias grep='grep --color=auto'
alias l='ls -CF'
alias la='ls -A'
alias ll='ls -alF'
alias ls='ls --color=auto'
```
观察清单，ll、la、l 这些"命令"其实不是命令，是别名——alias ll='ls -alF' 的意思是：当敲 ll，bash 悄悄把它替换成 ls -alF 再去执行。alias = 打字的快捷方式。

bash找命令的顺序是，先查别名表，再查内建命令，最后去查PATH里的文件

创建自己的第一个别名，给rm加一个安全网，虽然自己的文件删掉也不会有什么问题，但是可以自己加一道安全网rm -i

```
alias rm='rm -i'
touch /tmp/test
rm /tmp/test

rm: remove regular empty file '/tmp/test'?y
```
想用原版的话可以加上\，也就是\rm /tmp/test
把这个改动变成永久的
```
vim ~/.bashrc
alias rm='rm -i'
source ~/.bashrc
```
### 历史命令技巧
去查询下之前都做了什么操作
```
history | tail -10
  218  source ~/.bashrc
  219  myls
  220  alias
  221  alias rm='rm -i'
  222  touch /tmp/test
  223  rm /tmp/test
  224  vim ~/.bashrc
  225  source ~/.bashrc
  226  type rm
  227  history | tail -10
```
bash 把敲过的每条命令编号存档
再看一下文件权限，应该是只有我能读，因为会有敏感信息
```
ls -l ~/.bash_history
-rw------- 1 dev dev 5686 Aug 18 23:07 /home/dev/.bash_history
```
四个简单命令，
!!          # 再来一次，展开就是上一条命令，可用于忘了sudo
!$          # 上一条最后一个参数，可以省打好多目录
!N          # 从history输出中挑一个编号命令跑
ctrl-r      # 命令太多，只记得关键词，用这个搜历史命令

## D23
### 变量与引号
空格是参数的分割符
```
NAME="hello world"
echo $NAME
hello world
echo "$NAME"
hello world
```
观察发现两个输出一样，制造冲突
```
touch $NAME
ls
bin  hello  quiz.txt  replace.txt  vim-week-notes.md  words.txt  world
```
观察发现hello和world分开成了两个文件，因为变量展开后，shell 会对结果做词分割，按空格把内容拆成多个参数
试试带引号的命令
```
touch "$NAME"
rm "$NAME"
rm hello world    # 顺手清掉刚才词分割拆出来的两个散文件
```
rm只询问了一次，说明hello world当作了一个文件
变量展开后会被空格重新分割，用双引号裹住它才能保持完整。
注意一点，虽然双引号保住了整体不被分割，但是变量还是被取值了，如果只想打印出$NAME这个字符怎么办？用单引号，单引号把字符只当作字符用，不做额外引申

引号和通配符
```
echo *
bin quiz.txt replace.txt vim-week-notes.md words.txt
echo '*'
*
echo "*"
*
```

观察输出，说明*都被引号变成了字符，shell展开的流水线顺序大致是这样的
变量展开->词分割->通配符展开->引号摘除
引号的作用就是在内容外面画一个保护圈：圈内的东西，后面的流水线步骤一律跳过，所以双引号保住了空格，冻住了*
双引号只冻结词分割和通配符，不冻结 $；单引号连 $ 一起冻住。$ 走的是相反方向——激活执行，也就是命令替换。
$(...) 的意思是：先执行括号里的命令，把它的输出变成文字，塞回原位置。

```
echo 今天是 $(date)
今天是 Wed Aug 19 12:38:01 AM CST 2026
echo 今天是 `date`
今天是 Wed Aug 19 12:39:05 AM CST 2026
```

观察输出，发现 $ 激活执行了 date 命令，也就是 $(date) 和\`date\`做的事情是一样的
$(date)是现代写法，另一个是以前的老写法
看下面两个命令

```
echo "我是 $(whoami)，家目录在 $HOME"
我是 dev，家目录在 /home/dev
echo '我是 $(whoami)，家目录在 $HOME'
我是 $(whoami)，家目录在 $HOME
```
观察输出，双引号中执行了$的激活命令，而单引号中没有执行，所以如果在一句话中既要命令替换，又要保持整体性，应当使用双引号

## D24
### 退出状态码 $?
每条命令跑完后，会记录下状态：0 = 成功（及格），非 0 = 失败（0~255 之间的数字，不同数字代表不同死法），shell 把最近一条命令的执行状态存在特殊变量 $? 里。

```
ls /etc/passwd
/etc/passwd
echo $?
0

ls /tmp/study
ls: cannot access '/tmp/study': No such file or directory
echo $?
2
echo $?
0

```
观察两个$?的输出，发现第一个是执行成功，所以返回0，第二个失败，返回2，第三个成功返回0

下面介绍两个命令和$?类似，test和[
test是检查一个条件，不打印任何东西，把检查结果记录下来
```
test -f /etc/passwd
echo $?
0
test -f /etc/nonexistent
echo $?
1
ls -l /usr/bin/[
-rwxr-xr-x 1 root root 55744 Jan 23  2026 '/usr/bin/['
```
-f表示"这个路径是不是一个存在的普通文件"
观察结果发现$?输出改变了，说明，这个test只是记录，并不做多余操作，令人吃惊的是[居然是个真实存在的文件
\[用法和test一模一样，只是最后必须多一个\]
```
[ -f /etc/passwd ]
echo $?
0
[-f /etc/passwd ]       # 故意去掉[后的空格
[-f: command not found
```
观察去掉空格后的输出，发现 [ 本身就是一个命令名，所以必须和后面的东西空开；它和 test 是一对功能一样的孪生命令，只是 [ 最后必须配一个 ]

### if条件判断
if的语法是这样的
```
if 命令; then
    命令状态为 0 时走这里
else
    状态非 0 时走这里
fi
```
if 后面跟的是命令，不是数学表达式！if 会去执行那条命令，然后翻它的状态：0 → then 分支，非 0 → else 分支。\[ -f /etc/passwd \] 就是一条完整命令，它什么都不输出、只默默修改状态，正好给 if 当判断条件。
分号 ;——它和 |、&& 一样是命令分隔符，意思是"同一行里还有下一条命令"。写成两行的话分号可以不要，但习惯上 then 都跟在 if 行。
fi——if 倒过来写，bash 家的传统收尾标记（还有类似的，后面 for 循环会见到 done）。
```
if [ -f /etc/passwd ]; then
    echo "文件在"
else
    echo "文件不在"
fi
文件在
```
发现if判断生效
\[ \] 能用的不仅仅是 -f，还有以下内容
| 命令 | 意思 |
|---|---|
| -f 路径 | 普通文件存在 |
| -d 路径 | 目录存在 |
| -e 路径 | 存在（不挑类型）|
| 字符串1 = 字符串2 | 字符串相等 |
| 数字1 -eq 数字2 | 数字相等 |
| 数字1 -gt 数字2 | 数字大于 |

字符串比较
```
NAME=hello
if [ "$NAME" = "hello" ]; then
    echo "打招呼 $NAME"
else
    echo "不认识"
fi
打招呼 hello
```

### for循环
for循环语法和if差不多:
```
for 变量 in 列表; do
    循环体
done
```
执行流程：把 in 后面的列表按空格拆开，每项依次塞进变量、跑一遍循环体，列表跑完自动收工。done 是它的收尾标记。
```
for n in 1 2 3 4 5; do
    echo "第 $n 次"
done
第 1 次
第 2 次
第 3 次
第 4 次
第 5 次
```
### for+通配符
for循环中的判断语句如果少还好，可以穷举完，如果数量很多，穷举会很麻烦，这时候就需要用到通配符了
```
for f in *; do
    echo "发现：$f"
done
发现: bin
发现: quiz.txt
发现: replace.txt
发现: vim-week-notes.md
发现: words.txt
```
\* 被展开成当前目录（/home/dev）下所有文件/目录的名字
把if和for还有\*结合起来写个脚本
```
for f in *; do
    if [ -d "$f" ]; then
        echo "$f 是目录"
    else
        echo "$f 是文件"
    fi
done
bin是目录
quiz.txt是文件
replace.txt是文件
vim-week-notes.md是文件
words.txt是文件
```
观察输出，功能实现
### while循环
while和for的语法是一样的
```
while 命令; do
    循环体
done
```
写一个小脚本，用来计数
```
count=1
while [ "$count" -le 5 ]; do
    echo "第 $count 次"
    count=$((count + 1))
done
```
-le是小于等于、$((...))是算数拓展，里面是算式，不是命令
如果陷入死循环，可以用ctrl+c紧急停止

## D25
### 亲手写一个shell脚本
写脚本有三步：写文件，加执行权限，运行。
动手
```
# 先创建hello文件
vim ~/bin/hello

# 输入以下内容
#!/bin/bash
echo "hello, this is my first command"
echo "Today's date is $(date +%F)"
```
第一行 #!/bin/bash 叫 shebang（sharp # + bang ! 的合称）。# 在 bash 里本来是注释，但文件第一行的 #! 有特殊待遇：内核执行这个文件时，看到这行就知道"用 /bin/bash 来解释它"。没有这行，系统会用当前 shell 来跑，大概率也能跑，但这是脚本的身份证，必须写。
保存退出后，修改文件权限
```
chmod +x ~/bin/hello
hello
hello, this is my first command
Today's date is 2026-08-19
```
观察输出，发现可以运行，脚本完成
把身份鉴定机装进脚本，把它固化成一个叫 check 的命令
```
#!/bin/bash
for f in *; do
    if [ -d "$f" ]; then
        echo "$f 是目录"
    else
        echo "$f 是文件"
    fi
done
```
同样修改权限，然后运行，发现可以运行
但是check脚本有个短板，它只能读取当前目录下的文件，我想去读取别的目录呢？
需要给脚本加参数
ls -l /tmp这个命令是怎么拿到-l和/tmp的呢？
用的是位置参数
| 变量 | 含义 |
|---|---|
| `$1` | 第 1 个参数 |
| `$2` | 第 2 个参数 |
| `$#` | 一共几个参数 |
| `$0` | 脚本自己的名字 |
改造check:把循环行从for f in *改成接受参数
只需修改一行，把 for f in *; do 改成
```
for f in "$1"/*; do
```

最后一步，需要给脚本加上完成状态，失败了返回什么状态？
显然如果check后面不加参数应该是会出bug，所以给它加上守卫
```
if [ -z "$1" ]; then
    echo "用法: check 目录路径"
    exit 1
fi
```
加在循环语句之前

-z——"字符串是空的吗"，空 → 成立

exit 1——脚本主动修改状态并结束。
这样脚本就彻底变成了命令

### 最终版 check（复盘）
把三块拼起来——shebang、守卫、循环：

```bash
#!/bin/bash
if [ -z "$1" ]; then
    echo "用法: check 目录路径"
    exit 1
fi
for f in "$1"/*; do
    if [ -d "$f" ]; then
        echo "$f 是目录"
    else
        echo "$f 是文件"
    fi
done
```

跑起来看：

```
check /etc
/etc/adjtime 是文件
/etc/alternatives 是目录
...（省略）

check
用法: check 目录路径
```
