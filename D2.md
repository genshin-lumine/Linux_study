# 目标
- **6** 权限模型：rwx、chmod
- **7** chown/chgrp、umask、特殊权限（setuid/sticky）简介
- **8** 用户与组：useradd/groupadd，创建自己的非 root 账号
- **9** sudo 原理与 sudoers
- **10** 复盘日：权限场景小测

## 6
### rwx
首先理解文件权限，直接看例子，我是事先在test目录下建立了一个D6.md文件和一个subdir的目录
```
ls -l /tmp/test
-rw-r--r-- 1 root root    0 Aug 15 13:10 D6.md
drwxr-xr-x 2 root root 4096 Aug 15 13:09 subdir
```
把-rw-r--r--拆开:
第1位是文件类型，- 普通文件，d 目录，l 软链接
后面的9位分成三组，每组3位
第 1 组	是owner的权限，属于创建者的权限
第 2 组	是组的权限
第 3 组	是其他人对这个文件的权限
这三位固定是r(read读)、w(write写)、x(execute执行)，没有的用-占位

上面的D6.md我们可以看出来它是一个普通文件，权限是创建者拥有读写的权力，其它人只能读
subdir是一个目录文件，创建者拥有读写和访问的权限，其他人只有读和访问的权限

下面介绍一下数字记法，因为在很多配置文件中都是用到数字记法，了解很有必要
r=4、w=2、x=1，直接将每组间的数字相加即可，会得到一个三位数字
例如:

|数字	|含义|
|------|----|
|644|	rw-r--r-- 属主能读写，别人只能读（文件最常见的默认值）|
|755|	rwxr-xr-x 属主全能，别人能读能执行（目录、程序常见）|
|600|	rw------- 只有自己能读写（私钥、密码类文件）|
|700|	rwx------ 只有自己能进（~/.ssh 目录就是这个）|

r/w/x 对文件和目录的含义不一样


|权限	|对文件|	对目录|
|------|-----|-----|
|r	|能读文件内容|	能列出目录里有哪些文件（看名单）|
|w	|能修改文件内容	|能在目录里增删文件（改名单）|
|x	|能把它当程序执行	|能进入这个目录（cd、访问里面的文件）|

搭建试验场
```
mkdir /tmp/perm-test
cd /tmp/perm-test
touch hello.txt
mkdir sub
touch sub/file.txt
ls -l                    # 看看这几个文件的默认权限是多少

-rw-r--r-- 1 root root    0 Aug 15 15:00 hello.txt
drwxr-xr-x 2 root root 4096 Aug 15 15:01 sub

hello.txt：普通文件、创建者有读写权限、其它有读权限，只有一个链接，大小是0KB，属主和属组都是root
sub：文件夹文件、创建者有看名单/改名单/进门权限，其它人只有看名单/进门权限，有两个链接，大小是4096KB，属主和属组都是root
```
### 用符号法给文件分配权限
#### 符号法
chmod 有两种写法——符号法（u/g/o/a 加上 +/-/= 加上 rwx，适合"微调"）和数字法
u=user 属主、g=group 属组、o=others 其他人、a=all 全部；+ 加权限、- 减权限、= 精确设成
```
chmod u+x hello.txt     # 给属主(u)加(+)执行位(x)
ls -l hello.txt
-rwxr--r-- 1 root root    0 Aug 15 15:00 hello.txt

chmod go-w hello.txt    # 同时去掉组(g)和其他人(o)的写位
ls -l hello.txt
-rwxr--r-- 1 root root    0 Aug 15 15:00 hello.txt      # 没有的权限去掉也没影响，还是原样

chmod a+r hello.txt     # 给所有人(a)加读位
ls -l hello.txt
-rwxr--r-- 1 root root 0 Aug 15 15:00 hello.txt         # 已有的权限加上也没关系，还是原样
```
#### 数字法

```
chmod 755 hello.txt     # 7->rwx,5->r-x,权限应为rwxr-xr-x
stat hello.txt
  File: hello.txt
  Size: 0               Blocks: 0          IO Block: 4096   regular empty file
Device: 253,3   Inode: 196         Links: 1
Access: (0755/-rwxr-xr-x)  Uid: (    0/    root)   Gid: (    0/    root)
Access: 2026-08-15 15:00:43.105394715 +0800
Modify: 2026-08-15 15:00:40.668313005 +0800
Change: 2026-08-15 15:25:35.428399104 +0800
 Birth: 2026-08-15 15:00:40.668313005 +0800
# 对照access行，发现755对应rwxr-xr-x，符合预期

chmod 600 hello.txt     # 6->rw-,0->---,权限应为rw-------
stat hello.txt
Access: (0600/-rw-------)  Uid: (    0/    root)   Gid: (    0/    root)
# 符合预期

chmod 644 hello.txt     # 6->rw-,4->r--,权限应为rw-r--r--
stat hello.txt
Access: (0644/-rw-r--r--)  Uid: (    0/    root)   Gid: (    0/    root)
# 符合预期

```

### 目录权限实验
ls -l sub显示的是**sub里面文件**的权限，要看**sub自己的权限**需要加上-d
看下不同
```
ls -l sub   # 查看文件夹里面文件的详情
-rw-r--r-- 1 root root 0 Aug 15 15:01 file.txt
ls -ld sub  # 查看文件夹本体详情
drwxr-xr-x 2 root root 4096 Aug 15 15:01 sub
```
开始实验：
```
chmod 000 sub       # 把sub的权限全去掉
ls sub
file.txt            # 还可以访问文件夹内容，因为我是root，拥有最高权限
sudo -u nobody ls sub       # 以nobody的身份执行ls命令，是系统自带的身份
ls: cannot open directory 'sub': Permission denied      # 请求被拒绝了，说明权限管理生效了

chmod 400 sub       # 开放读权限
sudo -u nobody ls sub
ls: cannot open directory 'sub': Permission denied      # 只给了创建者权限，nobody不是创建者
chmod 404 sub       # 给nobody开放读权限
sudo -u nobody ls sub
file.txt            # 查到文件
sudo -u nobody ls -l sub
-????????? ? ? ? ?            ? file.txt                # 没有x权限，没法进入，查不到详情

chmod 101 sub       # 开放进入权限
sudo -u nobody ls sub
ls: cannot open directory 'sub': Permission denied      # 没有查看权限
sudo -u nobody cat sub/file.txt                         # 没有报错，可以抓取，说明能访问到内容，file.txt的权限是读写，sub文件夹权限是访问

chmod 755 sub       # 恢复文件
```
总结，目录的r和x是不同概念，有的时候不需要r权限也可以进入目录访问文件，文件能不能被 cat，取决于：路径上每一级目录都有 x + 文件自己有 r


## 7
上面学习了给文件和目录如何修改用户权限，下面学习怎么修改文件所属
### chown / chgrp
chown（change owner）和 chgrp（change group）用于修改用户所属
进行实验：
```
ls -n hello.txt            # -n：显示数字形式的属主属组
-rw-r--r-- 1 0 0 0 Aug 15 15:00 hello.txt   # 数字1是连接数，0，0，0是属主编号属组编号大小，系统里其实存的是编号（uid/gid），root 的编号是 0，nobody 是 65534，ls -n能看到原始数据

chown nobody hello.txt      # 把属主改成nobody
ls -l hello.txt
-rw-r--r-- 1 nobody root 0 Aug 15 15:00 hello.txt       # 成功改变

chgrp nogroup hello.txt     # 把属组改成nobody
ls -l hello.txt
-rw-r--r-- 1 nobody nogroup 0 Aug 15 15:00 hello.txt    # 成功修改

chown root:root hello.txt   # user:group 写法，一步同时改属主和属组，恢复原样
ls -l hello.txt
-rw-r--r-- 1 root root 0 Aug 15 15:00 hello.txt         # 成功复原
```
chown 一次只能改一个文件，改整个目录要加 -R
```
mkdir tree
touch tree/f1 tree/f2
chown -R nobody tree       # 目录和里面所有文件一起改,-R（递归）
ls -l tree                 # 确认 tree 和 f1、f2 都变了
-rw-r--r-- 1 nobody root 0 Aug 15 16:26 f1
-rw-r--r-- 1 nobody root 0 Aug 15 16:26 f2
chown -R root:root tree    # 恢复
ls -l tree
-rw-r--r-- 1 root root 0 Aug 15 16:26 f1
-rw-r--r-- 1 root root 0 Aug 15 16:26 f2
```
**普通用户不能 chown（把自己的文件"送给"别人也不行），只能 chgrp 到自己所在的组。
chown是只有root可以做的事情**

### umask
规则：新文件 = 666 − umask，新目录 = 777 − umask。当前 umask 是 022：
文件：666 − 022 = 644（本来 666 就没有 x，所以文件永远不默认带执行位——防止新建的文本文件意外变成"可执行程序"）
目录：777 − 022 = 755（目录需要 x 才能进，所以保留）

```
umask                    # 看当前值
0022
mkdir d1 && touch f1
ls -ld d1; ls -l f1
drwxr-xr-x 2 root root 4096 Aug 15 19:45 d1
-rw-r--r-- 1 root root 0 Aug 15 19:45 f1        # 755 和 644，符合公式

umask 077                # 换成严格模式："只有创建者自己有全部权限，别人什么都别想要"
mkdir d2 && touch f2
ls -ld d2; ls -l f2
drwxr-xr-x 2 root root 4096 Aug 15 19:45 d1
-rw------- 1 root root 0 Aug 15 19:46 f2        # 700 和 600

umask 022       # 恢复默认
```
077 = 扣掉属组和 others 的全部权限。它只影响之后新建的文件，已有的不受影响。注意 umask 是当前 shell 会话的属性（子进程继承父进程的），换一个终端登录还是默认值；想永久改要写进 ~/.bashrc

### 特殊权限：s和t是什么
```
ls -l /usr/bin/passwd
-rwsr-xr-x 1 root root 64152 May 30  2024 /usr/bin/passwd
ls -ld /tmp
drwxrwxrwt 15 root root 4096 Aug 15 15:00 /tmp/
```
这两个是特殊权限，放在数字法的第 4 位

|数字|	名字|	作用|	典型例子|
|---|----|----|-----|
|4	|setuid（u+s）	|程序执行时借用文件属主的身份，而不是执行者的身份	|/usr/bin/passwd 是 4755|
|2|	setgid（g+s）|	设在目录上：目录里新建的文件自动继承目录的属组|	共享项目目录 2775
|1|	sticky（o+t）|	设在目录上：只有文件主人（和 root）能删，别人即使有 w 也删不掉	|/tmp 是 1777|
为什么需要临时借用文件属主的身份呢？
因为在某些场景下，普通用户需要用到root身份修改一些内容，例如改密码，改密码需要用到passwd命令，存放在/etc/shadow里，但是shadow文件只有root有权改写，普通用户改密码只能临时借用root身份
为什么要设置sticky位呢？
因为/tmp文件夹是所有用户共用的临时目录，如果不限制删除的话，那会出现互相删文件的情况，所以只能往/tmp中放文件、也能删除自己的文件，但是删除不了别人的
验证sticky实验
```
touch /tmp/mine
sudo -u nobody rm /tmp/mine
rm: remove write-protected regular empty file '/tmp/mine'? yes
rm: cannot remove '/tmp/mine': Operation not permitted      # 操作没有批准，收到sticky保护

touch /tmp/perm-test/sacrifice
chmod 777 /tmp/perm-test
sudo -u nobody rm /tmp/perm-test/sacrifice
ls -l
drwxr-xr-x 2 root root 4096 Aug 15 19:45 d1
drwx------ 2 root root 4096 Aug 15 19:46 d2
-rw-r--r-- 1 root root    0 Aug 15 19:45 f1
-rw------- 1 root root    0 Aug 15 19:46 f2
-rw-r--r-- 1 root root    0 Aug 15 15:00 hello.txt
drwxr-xr-x 2 root root 4096 Aug 15 15:01 sub
drwxr-xr-x 2 root root 4096 Aug 15 16:26 tree
# 删除成功 —— 没有 sticky，目录的 w 就能删别人的文件

chmod 755 /tmp/perm-test         # 恢复实验场
```
验证setgid实验
```
mkdir /tmp/sgtest
chgrp nogroup /tmp/sgtest
chmod 2775 /tmp/sgtest
touch /tmp/sgtest/f_root
ls -l /tmp/sgtest/f_root
-rw-r--r-- 1 root nogroup 0 Aug 15 20:07 /tmp/sgtest/f_root     # 属组是nogroup，文件自动继承了目录的属组
```
注意看chmod 2775 /tmp/sgtest，这个命令变成了四位数
因为rwx权限模型不是9位，而是12位，完整模型如下：
[特殊权限 3 位] [属主 rwx] [属组 rwx] [others rwx]

|特殊权限位的值|特殊位|例子|
|------------|------|----|
|4|setuid|4755，s 出现在属主的 x 位  |
|2|setgid| 	2775，s 出现在属组的 x 位 |
|1|sticky| 	1777，t 出现在 others 的 x 位 |
|6|setuid+setgid|4+2|

那么在ls的时候只有rwx-的显示，这个表达是如何在rwx中显示的？
|情况|	ls 显示|
|---|-----|
|setuid| 位有 + 属主 x 也有	属主 x 位置显示小写 s|
|setuid |位有 + 属主 x 没有	显示大写 S|
|setgid| 同理	属组 x 位置 s / S|
|sticky |同理	others x 位置 t / T|

通常情况下，出现大写符号，说明配置有问题
动手验证一下
```
cd /tmp/perm-test
chmod u+s hello.txt      # setuid 加上,等价于4644
ls -l hello.txt
-rwSr--r-- 1 root root 0 Aug 15 15:00 hello.txt     # 出现了大写的S，说明没有x权限
chmod u+x hello.txt      # 补上属主的x，等价于4744
ls -l hello.txt
-rwsr--r-- 1 root root 0 Aug 15 15:00 hello.txt     # 变成小写s

stat -c '%a %A' hello.txt   # stat的%a会显示4位数，是系统里存的完整的值
4744 -rwsr--r--          # 等价于4744
chmod 644 hello.txt      # 恢复
```

## 8
### 创建非root账号
#### root有什么不好
敲错指令没有拦截，谁都有粗心的时候
所有操作不可追溯，用非root账号可以溯源，在/var/log/auth.log中
攻击者不容易突破
行业标准

#### 查看用户信息，/etc/passwd

```
head -3 /etc/passwd
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
grep nobody /etc/passwd
nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin
```
观察格式
```
root:x:0:0:root:/root:/bin/bash
 │   │ │ │  │    │       └── 登录后用的 shell
 │   │ │ │  │    └── 家目录
 │   │ │ │  └── 备注（全名等）
 │   │ │ └── 属组编号 gid
 │   │ └── 用户编号 uid
 │   └── 密码占位符（x = 密码不在这，在 /etc/shadow）
 └── 用户名
```
为什么密码要分开放：/etc/passwd必须全系统可读，所以密码哈希只能放别处。
```
ls -l /etc/shadow   # 密码存放在/etc/shadow中，只有root可读
-rw-r----- 1 root shadow 1093 Aug 13 20:38 /etc/shadow
sudo head -1 /etc/shadow    # 能看到一串$6$开头的乱码，是加密后的密码哈希
```
用户编号(uid)：0 = root；1~999 = 系统服务专用账号（nobody 是 65534，特殊值）；1000起 = 真实用户。这个约定让系统一眼能分清"人"和"服务"。

#### 建立账号：useradd
useradd -m -s /bin/bash dev
-m：创建家目录 /home/dev（不加 -m 就没有家目录，登录进去会报错）
-s /bin/bash：登录shell用bash（不加的话Ubuntu默认/bin/sh即dash，交互体验差）
验证
```
grep dev /etc/passwd          # 确认记录进了passwd，uid是1000
sys:x:3:3:sys:/dev:/usr/sbin/nologin
dev:x:1000:1000::/home/dev:/bin/bash
ls -ld /home/dev              # 家目录已建好，属主dev
drwxr-x--- 2 dev dev 4096 Aug 15 20:44 /home/dev/
ls -a /home/dev
.  ..  .bash_logout  .bashrc  .profile  # 里面已经有 .bashrc .profile 等文件
# 新用户家目录的"出厂配置"是从 /etc/skel（skeleton，骨架）复制来的。以后想给所有新用户统一预置配置（比如一份公共的 .vimrc），改 /etc/skel 即可。
passwd dev      # 设置密码，输入两遍，均不回显
```

#### 切换身份验证
```
su - dev
```
-表示登录式切换，完整加载dev环境配置，目录切到/home/dev中，不加-，会保留root环境变量和当前目录，容易出现问题。root切换到普通用户不需要密码，普通用户切换到root需要密码。
```
whoami
dev             # 现在你是谁
id
vuid=1000(dev) gid=1000(dev) groups=1000(dev)   # 所属主id，组id
pwd
/home/dev       # 当前工作环境
```
体验一下四处碰壁的感觉
```
ls /root
ls: cannot open directory '/root': Permission denied        # 系统拦截
touch /etc/test
touch: cannot touch '/etc/test': Permission denied          # /etc目录是root的
exit                  # 退回 root
```
#### 组groupadd / usermod
Ubuntu的约定：建用户时自动建一个同名主组（dev用户→dev组，这叫UPG用户私有组），所以日常文件属组都是自己的组。需要"把人拉进同一个组"时用附加组。
```
groupadd developers            # 建一个新组
grep developers /etc/group
developers:x:1001:          # /etc/group 是组的户口本，格式类似passwd
usermod -aG developers dev     # 把 dev 加进 developers 组
```
为什么必须-a：-G是"设置附加组列表"，不加-a（append）会直接覆盖——以后加了 3 个组，**一次忘记-a就全丢了。这是 Linux 圈最著名的坑之一。**

```
groups dev      # 列出dev属于哪些组
dev : dev developers
id dev          # 查看主组和附加组
uid=1000(dev) gid=1000(dev) groups=1000(dev),1001(developers)
```
把dev加入sudo组
```
usermod -aG sudo dev
groups dev
dev : dev sudo developers   # 成功添加
```

## 9
### sudo原理
dev是一个普通用户，为什么sudo可以让它干root的活儿呢？
查看一下sudo的权限
```
ls -l /usr/bin/sudo
-rwsr-xr-x 1 root root 277936 Mar  2 20:56 /usr/bin/sudo
```
发现它是一个带setuid的程序
所以流程应该是这样的，dev用户调用sudo命令，因为sudo有setuid权限，所以它可以暂用root的身份运行，sudo打开/etc/sudoers(查找授权名单)，检查有没有dev，如果有则以root身份执行命令；没有就拒绝

那么sudo和之前的su命令区别是
||	su|	sudo|
|---|--|--|
|身份变化|	整场会话换成目标用户|	只借单条命令，用完即还|
|要谁的密码|	目标用户（root）的密码|	你自己的密码|
|授权粒度	|全有或全无	|可以精确到"只允许某几条命令"|
|留痕	|有登录记录|	每条命令都有记录|
实验
```
su - dev
whoami
dev
sudo ls /root
apt-backup  snap  workspace       # 要求输入dev密码，成功执行
ls /root
ls: cannot open directory '/root': Permission denied    # 请求被拒绝
```
### 密码缓存机制
默认 sudo 输一次密码后 15 分钟内免密。免得每条命令都输一遍。时间戳记在当前终端会话里。
```
sudo -v     # -v:刷新时间戳
sudo whoami
root        # sudo是以root身份执行的
```
### 查看授权名单
```
# 退回root后
ls -l /etc/sudoers
-r--r----- 1 root root 1800 Jan 30  2024 /etc/sudoers
cat /etc/sudoers
# 重点查看以下行内容
Defaults        env_reset       # 执行命令前清空环境变量，防path注入攻击
Defaults        mail_badpass
Defaults        secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin"

root    ALL=(ALL:ALL) ALL       # root本人的行

%admin ALL=(ALL) ALL            # 旧版本遗留的组

%sudo   ALL=(ALL:ALL) ALL       # 关键行，sudo组成员拥有全部权限

@includedir /etc/sudoers.d      # 自己的规则放在这里
```
%sudo   ALL=(ALL:ALL) ALL怎么理解呢？
%sudo是授权对象，%开头=组，不带%是单个用户
ALL是指可以在哪些主机生效
(ALL:ALL)是指可以变身成哪些用户和组，也就是以任何组和人的身份执行
ALL是指能执行哪些命令
改sudoers必须用visudo而不是直接编辑：visudo保存前会做语法检查，写错了不让存。sudoers一旦语法错误，所有sudo全部拒绝工作，文件权限也必须是0440，为了防篡改设计

### sudo -u和sudo -i
```
sudo -u nobody id          # root可以变成任何人
sudo -i                    # 进入root的登录会话，干完exit退出
```
sudo -i效果和su -接近，区别是用自己密码+留痕，日常需要"长时间当 root 干活"时用它。

### 验证责任可追查
```
grep sudo /var/log/auth.log | tail -8
2026-08-15T22:10:15.681576+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo:session): session opened for user nobody(uid=65534) by root(uid=0)
2026-08-15T22:10:15.685464+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo:session): session closed for user nobody
2026-08-15T22:10:24.585214+08:00 iZuf61bjqah5v2cgy2iluqZ sudo:     root : TTY=pts/0 ; PWD=/root ; USER=root ; COMMAND=/bin/bash
2026-08-15T22:10:24.587276+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo-i:session): session opened for user root(uid=0) by root(uid=0)
2026-08-15T22:10:33.847161+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo-i:session): session closed for user root
2026-08-15T22:10:36.832075+08:00 iZuf61bjqah5v2cgy2iluqZ sudo:     root : TTY=pts/0 ; PWD=/root ; USER=root ; COMMAND=/bin/bash
2026-08-15T22:10:36.833118+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo-i:session): session opened for user root(uid=0) by root(uid=0)
2026-08-15T22:10:50.479008+08:00 iZuf61bjqah5v2cgy2iluqZ sudo: pam_unix(sudo-i:session): session closed for user root
```
可以看到每一条sudo记录

### sudo !!
简单来说就是给前一条语句加上sudo
举例，前一条命令是ls /root，普通用户没有权限，会被拒绝
运行完sudo !! 之后，会在终端执行 sudo ls /root这样就可以查看了

## 10
### 小测
Q1 小红（用户 xiaohong，属组 xiaohong）建了 report.md，权限 644。同事小明需要在里面追加内容，系统会允许吗？如果想给小明权限但不改文件属主，你会怎么做？

不允许，给小明一个xiaohong的附加组

**还要给文件补组写权限，因为文件权限是rw-r--r--**

Q2 一个程序以 www-data 身份运行，需要往 /var/log/myapp/ 目录里写日志文件。该目录属主是 root、权限 755。程序能写成功吗？为什么？最小改动怎么解决？

~~不行，因为/var目录是属于root的；把这个www-data身份放到sudoers组中，然后在命令前加上sudo即可~~

不行，因为程序需要在/var中w权限，www-data是属于others组，权限为r-x，没有w权限，因为它是个程序，所以最小的变动应该是chown www-data /var/log/myapp

Q3 dev 在 /tmp 里发现一个属主是 root 的文件 junk，dev 能删掉它吗？如果这个文件换到一个 777 且没有 sticky 位的目录里呢？

不能删，因为/tmp目录下的文件有sticky位；只要这个文件没有sticky位就可以删掉

Q4 某台服务器的 umask 是 027。新建一个目录和一个文件，默认权限各是什么？umask 027 的设计意图是什么？

~~目录：740、rwxr-----，文件是420，r---w----；让管理员只读，让用户去写~~

目录750，rwxr-x---，文件时640，rw-r-----让用户只能读这个文件，管理员可以修改

Q5 运维不小心执行了 chmod 755 /usr/bin/passwd，第二天全公司普通用户都改不了密码了。为什么？

~~setgid~~被修改没了

是setuid被修改没了

Q6 公司要求：运维同事 ops 只有"重启 nginx"这一件事能获得 root 权力，其他什么都不能干。用 sudoers 怎么实现？思路 + 规则大概怎么写？

~~不知道，没有学过~~

%ops ALL=(root) /usr/bin/systemctl restart nginx

%sudo ALL=(ALL:ALL) ALL

%ops是组名，ALL是指在哪个主机生效，(root)指的是让它以什么身份执行，/usr/bin/systemctl restart nginx这个是命令

翻译过来就是ops组用root身份执行/usr/bin/systemctl restart nginx命令

两个补充规矩：规则要写进 /etc/sudoers.d/ops 这个独立文件，而不是直接改主文件——模块化，坏一条不影响全局

命令必须写绝对路径 /usr/bin/systemctl：因为 sudoers 默认清空用户环境变量，写相对路径 systemctl 会找不到命令


### 清理旧内核
```
uname -r                          # 第一步永远是先确认
6.8.0-137-generic
ls /boot/vmlinuz-*                # 看到两个内核并存：5.15 和 6.8
/boot/vmlinuz-5.15.0-186-generic  /boot/vmlinuz-6.8.0-137-generic
dpkg -l | grep -E 'linux-(image|headers|modules)'    # 看 5.15 相关的包都叫什么
ii  linux-headers-5.15.0-186               5.15.0-186.196                                   all          Header files related to Linux kernel version 5.15.0
ii  linux-headers-5.15.0-186-generic       5.15.0-186.196                                   amd64        Linux kernel headers for version 5.15.0 on 64 bit x86 SMP
ii  linux-headers-6.8.0-137                6.8.0-137.137                                    all          Header files related to Linux kernel version 6.8.0
ii  linux-headers-6.8.0-137-generic        6.8.0-137.137                                    amd64        Linux kernel headers for version 6.8.0 on 64 bit x86 SMP
ii  linux-headers-generic                  6.8.0-137.137                                    amd64        Generic Linux kernel headers
ii  linux-image-5.15.0-186-generic         5.15.0-186.196                                   amd64        Signed kernel image generic
ii  linux-image-6.8.0-137-generic          6.8.0-137.137                                    amd64        Signed kernel image generic
ii  linux-image-generic                    6.8.0-137.137                                    amd64        Generic Linux kernel image

apt autoremove
0 upgraded, 0 newly installed, 5 to remove and 0 not upgraded.
# 没出现在清单里，说明被标记成手动安装，apt不敢碰，指定包名删除
apt remove linux-image-5.15.0-186-generic linux-headers-5.15.0-186 linux-headers-5.15.0-186-generic     # 已看到的为准

# 验证一下
ls /boot/vmlinuz-*
/boot/vmlinuz-6.8.0-137-generic                # 只剩 6.8 一个
dpkg -l | grep 5.15
rc  linux-image-5.15.0-186-generic         5.15.0-186.196                                   amd64        Signed kernel image generic
rc  linux-modules-5.15.0-186-generic       5.15.0-186.196                                   amd64        Linux kernel extra modules for version 5.15.0 on 64 bit x86 SMP
rc  linux-modules-extra-5.15.0-186-generic 5.15.0-186.196                                   amd64        Linux kernel extra modules for version 5.15.0 on 64 bit x86 SMP
df -h /                           # 顺便看看 /boot 和 / 释放了多少空间
Filesystem      Size  Used Avail Use% Mounted on
/dev/vda3        40G  8.2G   30G  22% /
```


### SSH登录切换到dev
先把公钥复制给dev
```
# 服务器上，root 会话
mkdir -p /home/dev/.ssh
cp /root/.ssh/authorized_keys /home/dev/.ssh/
chown -R dev:dev /home/dev/.ssh        # 换属主：钥匙是 dev 的了
chmod 700 /home/dev/.ssh
chmod 600 /home/dev/.ssh/authorized_keys
```
公钥登录验证的是"这把私钥配得上 authorized_keys 里的公钥"，公钥给谁，谁就信任这把私钥。
.ssh 目录 700、authorized_keys 600，一个都不能松，否则 sshd 会无视这个文件。
然后去本地修改config
```
code ~/.ssh/config
# User root  →  User dev
```
测试
```
ssh aliyun
whoami
dev             # 成功
```