# 目标
- **D16** vim 模式与基本编辑（i / Esc / :wq）
- **D17** vim 移动与高效编辑命令
- **D18** vim 搜索替换与多文件操作
- **D19** vim 配置：.vimrc 基本款
- **D20** 复盘日：用 vim 写一份学习笔记

## D16
### vim模式和基本编辑
首先安装vim正式版
```
sudo apt install vim

vim --version | head -5     # 看一下版本
```
vim的创建方式是，打开即创建，没有文件也不要紧
```
cd ~
vim hello.txt   # 不会报错
```
进入vim界面之后观察
左侧每行都有一个~，是虚拟行的占位符，代表文件的尽头
底部状态栏写着“hello.txt[new]”，表示文件名、这是个新文件
底部没有-- INSERT --字样，说明是在普通模式，是vim的默认模式
vim一共有三种模式
|模式|状态|可执行操作|
|---|---|-----|
|普通模式|刚打开时|移动光标、下命令、删除....|
|插入模式|按i进入|正常打字|
|命令行|按:进入|输入命令：保存、退出、查找...|
正常流程是，打开就是普通模式 → 按 i 进入插入模式打字 → 按 Esc 回到普通模式 → 按 : 进入命令行模式 → 输 wq 保存退出。
然后按下i，可以看到底部出现-- INSERT --字样，可以在里面写下文字
打完之后按下Esc，发现底部字样已经消失，说明回到了普通模式
然后按:，进入命令模式，输入wq两个命令的合体
>cat hello.txt
发现文件已经被写入
分析一下从打字到存储，文件经历了什么：内存缓冲区（vim 里改的）→ 按 :w 落盘到 hello.txt → :q 关闭。
a可以在光标后追加，i是在光标前插入，a和i用法一样
vim有个防呆设计，如果在q之前缓冲区没有保存改动的话，会被拒绝
>E37: No write since last change (add ! to override)
出现这个就应该知道自己没有保存，加上!的作用是告诉vim这个改动我不需要，要丢掉
用法是:q!，这样改动就会被删除，明确放弃改动

### 总结
vim 是模式化编辑器，底部状态栏是模式仪表盘；打开即创建；i 插入 / a 追加 / Esc 回普通模式 / :wq 保存退出 / :q! 丢弃退出。

## D17
### 移动

首先准备好一个words.txt的文件，内容包括
the quick brown fox jumps over the lazy dog
vim is a modal editor with three modes
today is the second day of vim week
practice makes perfect

#### 字符级移动

hjkl在vim中可以代替方向键，h代表←，j代表↓，k代表↑，l代表→
用起来会很别扭，用多了应该就好了吧，记住口诀：j在k下面，h在最左边，l在最右边

#### 词级移动

|键|	跳到哪|
|-|---|
|w	|下一个词的开头（word）|
|b|	上一个词的开头（back）|
|e|	当前/下一个词的结尾（end）|

#### 数字乘法

这是 vim 的通用法则：数字 + 命令 = 重复 N 次。所有移动、编辑命令都适用。
>3w等于一次跳 3 个词

#### 行级移动

|键|	跳到哪|
|-|-----|
|0	|行首（硬行首）|
|$	|行尾|
|^	|第一个非空白字符（有缩进时和 0 不一样）|
|gg|	文件第一行|
|G	|文件最后一行|
|3G|	第 3 行（数字+G = 跳到第 N 行）|

### 高效编辑命令
语法是
>编辑命令 = 动词 + 名词。动词是操作符（做什么），名词是移动（对什么做）。

先认识三个动词：

|动词|	含义|	例句|
|d|	delete 删除|	dw = 删一个词|
|c|	change 修改 = 删除 + 自动进入插入模式|	cw = 换一个词|
|y|	yank 复制	|yw = 复制一个词|

名词就是全部的移动:w $ e gg G 3G这种
可以自由拼装
>d2w(删掉2个词) d$(删到行尾) dG(删到文件尾)

有两个特殊的快捷方式，
dd = 动词连写两遍 = 对整行操作（删一行）。同理 cc 改整行、yy 复制整行
x = 删光标下的一个字符

练习一：x 和 u
光标移到第一行 quick 的 q 上，按 x——观察：q 消失，单词变成 uick
按 u——观察：撤销，quick 回来了
按 Ctrl-r——观察：重做，又变回 uick，再按 u 收尾
CTRL-r是u的反操作

练习二：dw 和 .
光标移到 lazy 的 l 上，按 dw——整个词被删掉
光标移到 quick 的 q 上，按 .——观察：上一次删除被原样重复，quick 也没了
. 重复的不是按键，而是"上一次修改"。改一次，然后到处按 .，同样的修改就发生在各个位置

练习三：dd + p = 移动一行（删除其实是剪切）
光标在第二行任意位置，按 dd——整行消失
按 p——观察：刚才删掉的那一行原样贴回来了
d 删掉的东西进了 vim 的寄存器（相当于剪贴板），p = paste 把它贴出来。所以"删除+粘贴"就是剪切

c操作
c（change）= 删除 + 自动进入插入模式。删旧词换新词是编辑里最高频的操作
cw——观察：整个单词消失，底部亮起 -- INSERT --，vim 自动把你送进插入模式。
光标移到任意一行，按 cc——整行被清空并进入插入模式，重新打一行字
c$——从光标删到行尾，自动进入插入模式
d 删完留在普通模式，c 删完再进插入模式。要改文字用 c，要移除文字用 d。

o/O操作
写文件时"在下面加一行"经常用到，o在光标下方开新行，O（shift+o）在上方开新行，都是自动进入插入模式

y操作
d操作删除的文字进入了剪贴板，dd+p是移动，原文没了，如果我想保存原文呢？
用y操作
yy 复制整行，在想插入的地方按p
yw复制一个词，在想插入的地方按p

### 总结

| 类别 | 命令 |
|---|---|
| 字符移动 | h j k l |
| 词级移动 | w b e |
| 行级移动 | 0 $ ^ gg G 3G |
| 数字乘法 | 3w 5j d2w 3dd |
| 动词 | d 删（=剪切） c 改 y 复制 |
| 整行操作 | dd cc yy |
| 粘贴/撤销 | p u Ctrl-r |
| 王牌 | . 重复上次修改 |
| 开新行 | o O |


## D18

### 搜索替换
#### 搜索 —— / ? n N
还是使用之前的words.txt文件做示范

普通模式按 /，打 the，回车——观察：光标跳到第一个 the，且全文所有 the 都高亮了
按 n 几次——观察：光标在全文的 the 之间巡游（n = next，下一个匹配）
按 N——往回跳（N = 上一个匹配）
试试 ?dog 回车——? 是向上找，从光标位置往文件头方向搜
最后按 :noh 回车——观察高亮消失（noh = no highlight，清除搜索高亮）
/ 和 ? 是一对方向，/ 往下找，? 往上找；n/N 顺着/逆着搜索方向继续。

#### 替换：s 家族

vim的替换命令和sed是同一套语法，s/旧/新/
先备份、养成好习惯
```
cp words.txt replace.txt
vim replace.txt
```
|命令|	范围|	数量|
|---|----|-----|
|:s/the/THE/	|当前行|	只换第一个|
|:s/the/THE/g	|当前行|	全换|
|:%s/the/THE/g	|全文件|	全换|
|:2,3s/the/THE/g	|第 2~3 行|	全换|
|:%s/the/THE/gc	|全文件|	逐个确认|

% 代表全文，g 代表 global 全换，c 代表 confirm 让我确认。

### 多文件操作
缓冲区不只放一个文件——vim 可以同时打开多个文件，每个占一个缓冲区，文件本体在磁盘，缓冲区在内存。:ls 就是缓冲区的花名册。

一次开两个文件
vim words.txt replace.txt
敲 :ls 回车——观察：列出 2 个缓冲区。注意每个前面的编号和标记（%a = 当前所在，# = 上一次所在的）

敲 :bn——切到下一个缓冲区（buffer next，观察底部文件名变了）

再 :bn——循环回来了；敲 :bp——往回切（buffer previous）

敲 :e hello.txt——不退出 vim，直接打开第三个文件（e = edit）

再 :ls——3 个缓冲区了

:e 让你在一个 vim 会话里穿梭多个文件，不用退出重进——这是 vim 作为"常驻工作台"的用法。

分屏
敲 :sp——观察：屏幕上下劈成两个窗口，显示同一个文件（sp = split）

试试在其中一个窗口里 j 移动——观察：两个窗口各滚各的，互不影响

敲 Ctrl-w w——观察：光标跳到另一个窗口（Ctrl-w 是窗口命令的前缀，后面再按 w = 切换）

再试 Ctrl-w j 和 Ctrl-w k——观察：向下/向上跳窗口——hjkl 的第三层身份：普通模式移动光标、Ctrl-w 后面就是窗口间移动，同一组键，不同语境

敲 :vsp——观察：左右劈成两半（vertical split）

敲 Ctrl-w q——关掉当前窗口；再 :q 关掉另一个

想全部退出：:qa!（quit all）

:sp/:vsp 后面还可以跟文件名（如 :sp replace.txt），直接分屏打开另一个文件

## D19
vim的配置

### .vimrc 基本款
vim 每次启动时，会自动去读主目录下的 ~/.vimrc，简单来说是一堆启动时自动执行的命令
比如:set hlsearch打开高亮，但是退出就没了，写入.vimc的话，每次打开vim这个配置都在
"开头是.vimrc的注释符
创建自己的.vimrc
```
vim ~/.vimrc        # 打开配置

# 输入如下配置
syntax on           " 语法高亮：按文件类型给代码上色
set number          " 显示行号：配合 3G/5G 跳行、报错定位
set hlsearch        " 搜索高亮：D18 挖的坑，今天永久填上
set incsearch       " 增量搜索：边打字边实时高亮
set ignorecase      " 搜索忽略大小写：搜 error 也匹配 Error
set smartcase       " 修正上一条：模式里含大写字母时自动恢复区分大小写
set showcmd         " 底部显示正在敲的命令：按 d 显示 d，再按 w 显示 dw
```

syntax on 和 set 是两类东西：syntax on 是开功能，set 选项 是设开关。syntax 高亮是代码编辑的刚需——关键字、字符串、注释一眼分色

number：行号是你跳转和沟通的坐标系

ignorecase + smartcase：一对兄弟配合——平时搜索宽容（少打 Shift），但你一旦在模式里打了一个大写字母，说明你是认真的，立刻恢复严格。这是"智能"二字的含义

showcmd：新手神器——按键后没反应时，看一眼左下角就知道命令敲到一半了

.vimrc 的本质是"启动命令清单"，核心就三点：

1、一次只加一行，加了立刻新开 vim 验证——出问题就是最后那行的事

2、在 vim 里改完不想退出，用 :source ~/.vimrc 让配置立即重载

3、改坏了不用怕：它只是个文本文件，最坏情况把整行删掉甚至删掉整个文件，系统毫发无伤——配置文件的错误永远可逆，这和改 /etc 系统配置的谨慎程度完全不同

## D20
小测验
建立quiz.txt文件，内容为
one two three
four five six
seven eight nine
需要依次做以下操作(不准进入插入模式完成2-5)

1、光标到 five 上，cw 把它改成 zero

2、dd 删掉第三行，再 p 把它贴到文件末尾

3、:%s/nine/9/g 全文替换

4、/two 搜索，n 巡游一遍，:noh 清高亮

5、yy + p 复制第一行到末尾

结果
```
one two three
four zero six
seven eight 9
one two three
```
