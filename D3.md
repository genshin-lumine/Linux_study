# 目标

- **D11** 管道与重定向原理：`|` `>` `2>&1`、文件描述符
- **D12** grep 基础与正则入门
- **D13** sed 常用操作
- **D14** awk 基础
- **D15** 复盘日：日志分析组合练习

## D11
### 管道与重定向
每个程序默认开着 3 个"通道"：
|通道	|名字|	编号（文件描述符 fd）	|默认连接|
|---|---|---|---|
|标准输入	|stdin|	0|	键盘|
|标准输出	|stdout|	1|	屏幕|
|标准错误	|stderr|	2|	屏幕|

那么什么是通道呢？文件描述符又是什么？
把每个程序想象成一个黑盒子，一旦启动，系统会自动连接三个管道
一个灌水，两个输出，stdin是负责灌数据的入口，默认连接的是键盘；stdout是输出正常结果的出口，stderr是输出错误信息的出口，它们俩默认连接的是屏幕。
那么为什么要把输出信息分开呢？一般来说程序正常输出就不会有错误信息，有错误也不会有正常输出了
因为unix中大量程序一次运行处理多个对象是常态，无法判断出来哪个部分出问题了，不同管道下游数据不一样，stdout是机器，要求数据干净，stderr是人，需要看到报错
文件描述符是进程打开的文件编号，每个进程出生时系统会自动分类三个编号，也就是0、1、2。
搞懂了通道，重定向也不难理解了，就是给这三个通道换一个接口。
下面进行实验
```
ls /home /root
/home:
dev
ls: cannot open directory '/root/': Permission denied   # /home目录下内容正常，/root下报错

ls /home /root > /tmp/out.txt
ls: cannot open directory '/root': Permission denied    # 正常输出内容没了，报错还存在
cat /tmp/out.txt
/home:
dev     # 正常信息被存储到out.txt文件中
```
发现 > 把正常结果写入文件，但是错误信息输出到屏幕中
尝试把两个信息放到一个文件中
```
ls /home /root > /tmp/mix.txt 2>&1
cat /tmp/mix.txt
/home:
dev
ls: cannot open directory '/root': Permission denied
```
发现两个信息均被写入到mix.txt中

从上文可以看到>是把1号管输出从屏幕重定向到文件中，
那么2号管也应该有自己的符号，就是2>
```
ls /home /root > /tmp/out.txt 2> /tmp/err.txt   # 把两个信息各自存入不同的位置

cat /tmp/out.txt    # 抓取看下内容
/home:
dev
cat /tmp/err.txt
ls: cannot open directory '/root': Permission denied
```
这就是脚本里常见的"分类归档"写法：数据一份、日志一份。
还有一个会用到的，有时候知道这里可能会报错，而且报错是正常的、我根本不想看，让屏幕干净。
```
ls /home /root > /tmp/out.txt 2> /dev/null
```
/dev/null 是 Linux 的黑洞设备——任何写进去的东西都会无声无息地消失，读了它永远返回"空"，相当于一个垃圾桶，明确的不想要的输出就可以丢给它

**2>&1**又是什么意思？
把 2 号管接到 1 号管此刻指向的地方，只有当前命令下是这样，之后不会跟着变
观察这条命令
>ls /home /root > /tmp/mix.txt 2>&1
如果反过来会出现什么结果？
```
ls /home /root 2>&1 > /tmp/trick.txt
ls: cannot open directory '/root': Permission denied

cat /tmp/trick.txt
/home:
dev
```
结果显而易见，2号管没有接到1号管指向的地方，所以它们有一个从左到右顺序执行的问题
2>&1先执行：2 号管接到 1 号管此刻的位置——此刻 1 号管还插在屏幕上；> /tmp/trick.txt 后执行：1 号管改插文件——但 2 号管已经复制完了，不跟着走；所以2 号管留在屏幕，报错照旧上屏
可以简写&>，上述的命令可以简写为
>ls /home /root &> /tmp/mix.txt
不会有顺序问题，&表示后面跟着的是文件描述符，不是文件名，所以会写成2>&1，而不是2>1

### |管道
|的本质是把A的stdout直接接到B的stdin中
>A|B
就是把 A 的 1 号管拔下来，直接插到 B 的 0 号管上。数据在两个程序之间直接流动，不经过磁盘。
老办法是创建一个临时文件用于中转，
```
A > /tmp/tmp.txt     # 先落地到磁盘
B < /tmp/tmp.txt     # 再读出来
```
管道法的优势：不落盘（快）、不留垃圾文件、B 可以边收边处理。这也是"一切皆文件"哲学的威力——管道两头只是普通的文件描述符，接上就能流。
使用管道
```
ls /home /root | wc -l
ls: cannot open directory '/root': Permission denied
2
```
输出结果为报错信息和一个数字
wc -l从管道中拿到的是stdout信息，是干净的数据，数出行数
管道只传1号管，也就是输出数据的信息
把报错信息传给null
```
ls /home /root 2>/dev/null | wc -l
2   # 输出干净
ls /home /root | wc -l 2>/dev/null
ls: cannot open directory '/root': Permission denied
2   # 为什么不行呢？
```
>ls /home /root | wc -l 2>/dev/null
这条命令的输出数据不符合预期，**因为ls和wc是两个不同的进程，也就是有两套管道，2>/dev/null的重定向只修改紧贴着的那个进程**
链式管道
```
ls -l /tmp | head -3
total 48
-rw------- 1 root root    0 Aug 13 21:27 AliyunAssistClientSingleLock.lock
srwxr-xr-x 1 root root    0 Aug 13 21:27 aliyun_assist_service.sock
```
三个零件组成流水线：ls -l 吐数据 → head 只取前 3 行 → 屏幕。一个命令的输出是下一个命令的输入，层层加工，中间没有任何临时文件。
**<符号**
既然1、2号管都有自己的重定向，那么0号管也应该有，<就是0号管的重定向符号
```
wc -l < /tmp/out.txt
2
```
输出结果与cat /tmp/out.txt | wc -l一样，<是直接把文件接给wc当0号管
总结：
|fd	符号	|作用|
|---|------|-----|
|0 (stdin)|	<|	从文件读入|
|1 (stdout)|	>|	输出到文件（覆盖）|
|2 (stderr)	|2>|	错误输出到文件|

## D12
### grep
grep全称是**g**lobally search for a **r**egular **e**xpression and **p**rint（全局搜索正则表达式并打印）
grep 在文本里按"模式"逐行搜索，把匹配的行打印出来
基本用法是：grep 模式 文件
使用grep
```
grep dev /etc/passwd
sys:x:3:3:sys:/dev:/usr/sbin/nologin
dev:x:1000:1000::/home/dev:/bin/bash
```
观察输出，发现有两条，grep做的事情是把/etc/passwd中的每一行拿来和dev对比，包含dev的行打印出来，尝试别的字符串
```
grep /bin/bash /etc/passwd
root:x:0:0:root:/root:/bin/bash
dev:x:1000:1000::/home/dev:/bin/bash
```
发现两条，并且终端中有匹配字符的高亮显示
将grep与管道相结合
```
ls /usr/bin | grep python
pybabel-python3
python3
python3.12
python3.12-config
python3-config
x86_64-linux-gnu-python3.12-config
x86_64-linux-gnu-python3-config
```
ls /usr/bin这条命令会输出很多文件，通过管道穿给grep，grep再将包含python的行筛选出来输出到屏幕上，组合一下就是“系统里装了哪些python相关命令”这个意思
#### grep的一些参数
四个常用的参数，-i、-v、-c、-n
```
ls /usr/bin | grep -i ssh      # 忽略大小写
ssh
ssh-add
ssh-agent
ssh-argv0
ssh-copy-id
ssh-import-id
ssh-import-id-gh
ssh-import-id-lp
ssh-keygen
ssh-keyscan

grep -v nologin /etc/passwd     # 反选，invert打印不匹配的行，排除所有禁止登录的系统用户
root:x:0:0:root:/root:/bin/bash
sync:x:4:65534:sync:/bin:/bin/sync
pollinate:x:105:1::/var/cache/pollinate:/bin/false
tss:x:109:116:TPM software stack,,,:/var/lib/tpm:/bin/false
lxd:x:999:100::/var/snap/lxd/common/lxd:/bin/false
dhcpcd:x:115:65534:DHCP Client Daemon,,,:/usr/lib/dhcpcd:/bin/false
dev:x:1000:1000::/home/dev:/bin/bash

grep -c nologin /etc/passwd     # 计数，只报告数字，系统里有几个nologin用户
30
grep -n root /etc/passwd        # 输出每行带原文行号
1:root:x:0:0:root:/root:/bin/bash
```
看日志时 -v 超级常用：grep -v debug xxx.log 把刷屏的 debug 行过滤掉。

### 正则入门
真实需求往往是模糊的："以 root 开头的行""以 bash 结尾的行"——这就需要正则表达式，用特殊符号描述一类文本，grep默认把模式当作正则来理解
先看下之前的实验结果
```
ls /usr/bin | grep python
pybabel-python3
python3
python3.12
python3.12-config
python3-config
x86_64-linux-gnu-python3.12-config
x86_64-linux-gnu-python3-config
```
发现它是把所有与python有关的都选中了，也就是“包含”python，如果我只想要“开头是”python呢？
用^这个符号
```
ls /usr/bin | grep ^python
python3
python3.12
python3.12-config
python3-config
```
可以发现少了很多输出，说明^可以把“包含”改成“开头是”
既然有开头是，自然有结尾是
```
grep bash$ /etc/passwd
root:x:0:0:root:/root:/bin/bash
dev:x:1000:1000::/home/dev:/bin/bash
```
成功选定
观察两个命令，发现符号位置并不确定，说明符号位置与语义表达关系很大，^放在开头，$放在结尾

正则剩下的几个符号
.   任意字符
[]  字符类
*   重复
\   转义
```
ls /usr/bin | grep 'python3.'
python3.12
python3.12-config
python3-config
x86_64-linux-gnu-python3.12-config
x86_64-linux-gnu-python3-config

ls /usr/bin | grep 'python3\.'
python3.12
python3.12-config
x86_64-linux-gnu-python3.12-config
```
观察上面命令，发现结果有很大区别，分析一下原因
.是匹配任意一个字符，所以在第一条命令中，它做的事情是在/usr/bin目录下找到所有与“python3”匹配的内容
\是转义字符，是把符号打回原形，告诉系统就把.看作是字面意思，不是特殊含义，所以它只匹配了“python3.”的内容
注意，不管是.还是\它们都是用在字符串里面的，而不是像^和$一样放在字符串外面
```
grep '^[rs]' /etc/passwd
root:x:0:0:root:/root:/bin/bash
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
systemd-network:x:101:102:systemd Network Management,,,:/run/systemd:/usr/sbin/nologin
systemd-resolve:x:102:103:systemd Resolver,,,:/run/systemd:/usr/sbin/nologin
systemd-timesync:x:104:105:systemd Time Synchronization,,,:/run/systemd:/usr/sbin/nologin
syslog:x:106:113::/home/syslog:/usr/sbin/nologin
sshd:x:113:65534::/run/sshd:/usr/sbin/nologin
```
观察输出，发现[]表示匹配括号中任意元素，^是在开头匹配，所以这条命令筛选的是所有以r和s为首的内容，[]内还可以添加范围
```
ls /usr/bin | grep 'python3\.[0-9]'
python3.12
python3.12-config
x86_64-linux-gnu-python3.12-config
```
[0-9]是指0到9的任意一个数字，[a-z]、[0-9a-f] 都是常用写法
```
echo "pyon"    | grep 'pyt*on'   # t 出现 0 次，也匹配！
pyon
echo "pyton"   | grep 'pyt*on'   # t 出现 1 次
pyton
echo "pyttton" | grep 'pyt*on'   # t 出现 3 次
pyttton
```
\*是“0次或者多次”匹配，并且打印的是整行，所以上述命令并不会出现多个回文，\*是为了表达“可选”这个概念
如果要表达匹配1次或者更多用+，要表达匹配0次或者1次的用?
```
echo "color"   | grep 'colou*r'
color
echo "color"   | grep 'colou\+r'    # 无输出

echo "colouur" | grep 'colou\?r'    # 无输出

```
.*组合表示任意字符重复任意次数
```
grep 'root:.*bash' /etc/passwd
root:x:0:0:root:/root:/bin/bash
```
只规定了以root:开头，中间任意内容，以bash结尾
### 总结
grep = 按模式逐行筛文本，字面量模式 → 正则模式，从"包含"升级到"描述一类行"。
选项各管一事：-i 容错大小写、-v 排除噪声、-c 只要数字、-n 定位行号、-o 看片段。
两个坑：正则 * ≠ shell 通配符 *；模式加单引号。
## D13
### sed
sed=**s**tream **e**ditor(流编辑器)
它与vim的区别在于，vim是人一条条命令交互编辑；sed是非交互的，给它一个指令单，按行处理，吐出结果。
所以sed的定位是属于管道中间，上游是给数据，sed处理，再交给下游。
基本语法：
sed '地址 动作' 文件
地址：要对哪些行动手
动作：干什么（p打印、d删除、s替换...）
看一下sed输出什么
```
sed 'p' /etc/passwd
root:x:0:0:root:/root:/bin/bash
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
sync:x:4:65534:sync:/bin:/bin/sync
games:x:5:60:games:/usr/games:/usr/sbin/nologin
games:x:5:60:games:/usr/games:/usr/sbin/nologin
```
sed会有个默认行为，每处理一行就打印一遍原样，而p又要求打印一边，所以就每行打印了两次
```
sed -n '1p' /etc/passwd
root:x:0:0:root:/root:/bin/bash
```
不再出现两行了，因为-n关闭了默认打印
```
sed -n '1,5p' /etc/passwd      # 第 1~5 行
sed -n '5,$p' /etc/passwd      # 第 5 行到最后一行（$ 在 sed 里=最后一行）
```
### 删除动作d和用正则当地址
明确一个观念，sed默认不修改文件，只是在中间环节加工数据
```
sed '3d' /etc/passwd | head -5
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
games:x:5:60:games:/usr/games:/usr/sbin/nologin
dev@iZuf61bjqah5v2cgy2iluqZ:~$ head -5 /etc/passwd

head -5 /etc/passwd
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
```
3d表示在地址3的位置执行动作d，也就是第3行不输出。
观察输出发现缺少了bin那一行输出，其次发现文件完好无损
地址除了行号，还可以用正则，用正则需要/模式/这样包住
```
sed -n '/root/p' /etc/passwd
root:x:0:0:root:/root:/bin/bash
sed '/nologin/d' /etc/passwd | wc -l
7
```
这两条指令等价于
grep 'root' /etc/passwd
grep -v 'nologin' /etc/passwd | wc -l

### s替换
语法：sed 's/旧/新/'，**默认每行只替换第一个**
```
echo "one two one" | sed 's/one/1/'
1 two one
echo "one two one" | sed 's/one/1/g'
1 two 1
```
观察输出发现，替换只替换第一个，第二条命令加上了g才把一行内所有的匹配全换了
修改文件是不可逆改动，宁可少干不可多干，所以默认只能替换第一个匹配
```
echo "/usr/local/bin" | sed 's|/usr|/home|'
/home/local/bin
```
处理路径时，可以任选一个字符当分隔符这里是 |，只要它不在你要匹配的文本里
```
sed '/^#/d; /^$/d' /etc/ssh/sshd_config
Include /etc/ssh/sshd_config.d/*.conf
KbdInteractiveAuthentication no
UsePAM yes
X11Forwarding yes
PrintMotd no
AcceptEnv LANG LC_*
Subsystem sftp  /usr/lib/openssh/sftp-server
UseDNS no
SyslogFacility AUTHPRIV
PermitRootLogin yes
PasswordAuthentication yes
```
观察输出，发现只有有效的行记录下来了，这个是运维看配置时常用的命令
-i用于写回文件
**必须要先备份**
```
cp /etc/passwd /tmp/passwd.bak
sed -i 's/root/ROOT/' /tmp/passwd.bak
grep ROOT /tmp/passwd.bak
ROOT:x:0:0:root:/root:/bin/bash
grep root /etc/passwd | head -3
root:x:0:0:root:/root:/bin/bash
```
观察输出，发现备份文件内容已经修改，原文件内容没变

### 总结
sed中最重要的是s和-i参数，s是替换，-i是写回命令
>s命令使用方法:sed 's/old/new/' addr/file

## D14
### awk基础
sed是按行加工数据，awk是按列加工数据，专门应对表格式数据(passwd文件、ls -l输出、日志)等
结构是：awk '模式 {动作}' 文件
处理模型如下：
```
读入一行 → 按分隔符切成若干"字段" → 匹配模式的行，执行动作 → 下一行
           $1  $2  $3  ...  $NF               $0 = 整行
```
$1、$2、$3：第 1、2、3 列（awk 里叫"字段"）
$0：整行
NF：这一行被切成了几列（Number of Fields），所以 $NF = 最后一列
```
awk '{print $1}' /etc/passwd | head -5
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
```
观察输出，本来应当只输出用户名的，但是却是整行输出，原因是awk默认的分隔符是空白，不是冒号
可以指定分隔符，命令是-F（Field separator）
```
awk -F: '{print $1}' /etc/passwd | head -5
root
daemon
bin
sys
sync
```
观察输出，只输出用户名，目的达到
```
ls -l /tmp | awk '{print $1}' | head -5
-rw-------
srwxr-xr-x
-rw-rw-r--
-rw-rw-r--
```
抽出了所有文件的权限列
### 多列组合、$NF 技巧、以及 awk 的"模式"
```
awk -F: '{print $1, $7}' /etc/passwd | head -5
root /bin/bash
daemon /usr/sbin/nologin
bin /usr/sbin/nologin
sys /usr/sbin/nologin
sync /bin/sync
```
print后面写多个字段、用逗号隔开，输出时awk自动会在中间补空格
等价于下面这条命令
>awk -F: '{print $1, $NF}' /etc/passwd | head -5
因为$NF是指的最后一列，passwd的每行都是7列，所以不用数也可以
awk的模式
>awk -F: '/bash/ {print $1}' /etc/passwd
/bash/是模式（匹配的字符串），{print $1}是动作，切出第1列，bash可以是正则表达式
模式还可以是数值条件
>awk -F: '$3 >= 1000 {print $1}' /etc/passwd
>nobody
>dev
$3是第三列uid，大于等于1000就代表是真实用户，nobody是65534
### 总结
awk读行 → 按分隔符切列（\$1 \$2 ... \$0 整行，NF 列数，\$NF 尾列）→ 模式筛行（正则/条件/无）→ 动作输出。

## D15
## 小测
Q1:ls /root 2>&1 | wc -l 和 ls /root | wc -l 2>&1，哪个会把错误信息也数进结果？为什么？
第2条指令，因为第2条指令2>&1在管道后，所以报错信息也一起被输出到wc中，所以wc数了错误信息
Q2:grep -c error xxx.log 和 grep -o error xxx.log | wc -l 结果会一样吗？
不一样，-c是数错误信息的行数；-o是匹配每一个错误的信息，然后传给wc，数量大概率不同
Q3:sed 's/a/b/' 一行里有 3 个 a，换几个？要全换加什么？
换第一个，在/a/b/后加g
Q4:awk -F: '{print $NF}' /etc/passwd 输出什么？
输出的每一行的最后一列

## 实战：调查 SSH 暴力破解
场景：你是这台服务器的管理员，怀疑有人在暴力猜密码登录。用日志查清楚：有没有攻击？多少次？来自哪里？
数据源是 /var/log/auth.log——SSH 认证日志（每次登录尝试都会记录，失败也会记）。

题1：先侦察——看有没有失败记录

```
sudo grep -i 'failed password' /var/log/auth.log | head -5
2026-08-16T03:21:13.363492+08:00 iZuf61bjqah5v2cgy2iluqZ sshd[16455]: Failed password for root from 91.92.40.44 port 49928 ssh2
2026-08-16T06:17:15.046221+08:00 iZuf61bjqah5v2cgy2iluqZ sshd[16760]: Failed password for invalid user ansible from 121.22.99.2 port 56202 ssh2
2026-08-16T23:45:50.032874+08:00 iZuf61bjqah5v2cgy2iluqZ sudo:      dev : TTY=pts/0 ; PWD=/home/dev ; USER=root ; COMMAND=/usr/bin/grep -i 'failed password' /var/log/auth.log
```

分析记录，前两条属于真实攻击，直接猜root失败了，下面它用ansible猜我用户名，也错了，说明是自动化扫描脚本在撒网，第三行是我使用sudo的时候被记录auth.log了

题 2：统计失败次数——但要排除"你自己"
```
sudo grep 'Failed password' /var/log/auth.log | wc -l
3
sudo grep 'sshd.*Failed password' /var/log/auth.log | wc -l
3
```

两个数字一样，因为 grep 默认区分大小写：大写 Failed password 匹配不到 sudo 记录里的小写 'failed password' 文本

大小写敏感验证：
```
sudo grep 'failed password' /var/log/auth.log | wc -l     # 小写：2（自己的 sudo 痕迹）
sudo grep 'Failed password' /var/log/auth.log | wc -l     # 大写：3（sshd 攻击记录）
sudo grep -i 'failed password' /var/log/auth.log | wc -l  # -i 忽略大小写：7（全都要）
```
grep 默认大小写敏感，Failed ≠ failed，-i 才忽略大小写。数字多出来的部分就是自己各种小写 sudo 记录——每跑一次 sudo 查询，日志就多一条痕迹
题 3：提取攻击者 IP
```
sudo grep 'sshd.*Failed password' /var/log/auth.log | awk '{print $9}'
91.92.40.44
ansible
;
;
```
结果不是全部都是ip地址，说明切错了列，为什么会出现两个;?
因为在使用sudo的时候，auth.log中记录了我的访问记录，而且题2/题3自己敲的命令文本里就含 "sshd" 和 "Failed password"，被模式 'sshd.*Failed password' 匹配到了

点睛：你在搜的，是自己上一秒敲的命令——grep 分不清"sshd 产生的记录"和"命令文本里提到 sshd 的记录"。日志排查永远要先排除自己的痕迹（grep -v 'sudo:'）

正确的切分方法，先排除sudo行
```
sudo grep 'sshd.*Failed password' /var/log/auth.log | grep -v 'sudo:'   # 排除sudo:
2026-08-16T03:21:13.363492+08:00 iZuf61bjqah5v2cgy2iluqZ sshd[16455]: Failed password for root from 91.92.40.44 port 49928 ssh2
2026-08-16T06:17:15.046221+08:00 iZuf61bjqah5v2cgy2iluqZ sshd[16760]: Failed password for invalid user ansible from 121.22.99.2 port 56202 ssh2

sudo grep 'sshd.*Failed password' /var/log/auth.log | grep -v 'sudo:' | awk '{for (i=1; i<=NF; i++) if ($i=="from") print $(i+1)}'
91.92.40.44
121.22.99.2
```
逐字翻译这个 awk 程序：for (i=1; i<=NF; i++) 从左到右检查每个字段；if($i=="from")当前字段的内容是 "from" 吗；print $(i+1) 是就打印它的下一个字段。不管 "invalid user ansible" 占几列，IP 永远在 from 后面

sort 和 uniq：排序与去重计数

sort:排序，默认按字典序；-n 按数字大小排；-r 倒序

uniq -c：合并相邻的重复行，并计数。关键前提：相同的行必须相邻——所以永远先 sort 再 uniq，因为"排序"就是让相同的行聚在一起。

标准三段式：sort | uniq -c | sort -rn——排序 → 计数 → 按数量倒序。这是 Linux 里"出现次数排行榜"的通用流水线。

题 4：完整组合拳——谁扫你最凶
```
sudo grep 'sshd.*Failed password' /var/log/auth.log | grep -v 'sudo:' | awk '{for (i=1; i<=NF; i++) if ($i=="from") print $(i+1)}' | sort | uniq -c | sort -rn

      1 91.92.40.44
      1 121.22.99.2
```
说明都是只攻击了一次

题 5：进阶统计——auth.log 里谁最活跃
```
sudo awk '{print $3}' /var/log/auth.log | sed 's/\[.*\]//' | sort | uniq -c | sort -rn | head -5

    350 CRON:
    207 sshd:
     47 sudo:
      1 systemd-logind:
      1 (systemd):
```
awk '{print $3}'：取第 3 列——进程名（D14）

sed 's/\\[.\*\]//'：把 [16455] 这类 PID 删掉，让所有 sshd 记录合并成同一个名字 sshd:（D13 替换 + D12 正则 .*）

sort：让相同进程名相邻

uniq -c：合并计数

sort -rn：按次数倒序

head -5：看前五名

